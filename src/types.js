// Type definitions for documentation purposes only
// JavaScript doesn't have static types, these are just JSDoc comments

/**
 * @typedef {Object} SlideContent
 * @property {string} title - The slide title
 * @property {string} [subtitle] - Optional subtitle
 * @property {string[]} points - Array of bullet points
 * @property {string} [visualHint] - Optional visual hint description
 * @property {string} [footer] - Optional footer text
 */

/**
 * @typedef {Object} DeckData
 * @property {string} companyName - Company name
 * @property {string} tagline - Company tagline
 * @property {SlideContent[]} slides - Array of slides
 */

// Export empty object since these are just type definitions
export {};
